[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "border-surveillance"
version = "0.1.0"
description = "Border surveillance AI pipeline"
authors = [{name="Shyam Gohel"}]
dependencies = []
